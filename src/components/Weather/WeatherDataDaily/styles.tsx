import styled from '@emotion/styled';

const DailyWeatherContainer = styled.div({
  display: 'flex',
  flexDirection: 'row',
  justifyContent: 'space-evenly',
  flexWrap: 'wrap',
});

export { DailyWeatherContainer };
