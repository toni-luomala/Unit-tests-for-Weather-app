import styled from '@emotion/styled';

const Column = styled.div({
  display: 'flex',
  flexDirection: 'column',
  width: '100%',
  maxWidth: '300px',
  marginLeft: '10px',
});

export default Column;
